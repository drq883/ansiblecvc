Understanding ansible.cfg

  /etc/ansible/ansible.cfg - host defaults

  ./ansible.cfg - directory (project) default

    [defaults]
    inventory = inventory
    remote_user = ansible
    host_key_checking = false

    [privilege_escalation]
    become = True
    become_method = sudo
    become_user = root
    become_ask_pass = False

  ansible all -m command -a "ls -a /root"
